{
  "name": "game",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "pnpm typecheck && node esbuild.cjs",
    "watch": "pnpm typecheck && node esbuild.cjs --watch",
    "typecheck": "tsc --noEmit -p src/client/tsconfig.json && tsc --noEmit -p src/server/tsconfig.json"
  },
  "devDependencies": {
    "@citizenfx/client": "2.0.7345-1",
    "@citizenfx/server": "2.0.7345-1",
    "@types/node": "^20.11.6",
    "esbuild": "^0.19.12",
    "typescript": "5.3.3"
  }
}
